services:
  - type: cron
    name: elasticsearch-sync
    envVars:
      - fromFile: .env
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python cron_sync.py"
    schedule: "*/10 * * * *"
